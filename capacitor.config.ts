import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dds.webapp',
  appName: 'datadrivenstrengthwebapp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
